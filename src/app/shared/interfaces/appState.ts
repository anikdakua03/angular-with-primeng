export interface AppState {
    configActive?: boolean;
    menuActive?: boolean;
    newsActive?: boolean;
    announcement?: any;
    // preset?: string,
    // primary?: string,
    // surface?: string,
    // darkTheme?: boolean
}