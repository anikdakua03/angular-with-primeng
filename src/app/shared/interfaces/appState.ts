export interface AppState {
    configActive?: boolean;
    menuActive?: boolean;
    newsActive?: boolean;
    announcement?: unknown;
    // preset?: string,
    // primary?: string,
    // surface?: string,
    // darkTheme?: boolean
}